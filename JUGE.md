# Juge

QUELLE `os` ne dit pas quantique. `qrng`/`qkd` exigent un appareil nommé.
Cette rail nomme la question. Elle ne juge pas les quatre. Elle ne frappe pas `quantique`.

Après famille juge.v0 et mode-protocol `5e4518b`, `quelle: os` reste classique.

Commande publique : `python3 quelle.py tirer|lire`.
QUANTUM signe plus tard. Les clés restent hors Git. Ce dépôt n'est pas le nœud QUANTUM.

Contrat : [famille/schema/juge.v0.json](https://github.com/carllaliberte/famille/blob/main/schema/juge.v0.json)
