# Juge

QUELLE `os` ne dit pas quantique. `qrng`/`qkd` exigent un appareil nommé.
Cette rail nomme la question. Elle ne juge pas les quatre.

Contrat : [famille/schema/juge.v0.json](https://github.com/carllaliberte/famille/blob/main/schema/juge.v0.json)
CLI : `python -m quantum peut-dire --fichier carte.json` — exit 0 | 2
